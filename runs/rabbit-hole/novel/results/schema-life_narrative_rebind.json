{
  "name": "life_narrative_rebind",
  "description": "Rebind a model-bound narrative graph to a successor model revision as one complete immutable graph revision. Reads the whole graph with the supplied scopes, refuses partial projections and unrelated models, keeps every node including historical assessments, drops only edges anchored to predecessor model hashes, and submits the successor through the existing revise operation. Record fresh depth assessments against the new model before committing further prose.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "requestId": {
        "type": "string",
        "minLength": 1,
        "maxLength": 256
      },
      "graphHash": {
        "type": "string",
        "minLength": 64,
        "maxLength": 64
      },
      "modelHash": {
        "type": "string",
        "minLength": 64,
        "maxLength": 64
      },
      "accessScopes": {
        "default": [],
        "maxItems": 32,
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1024
        }
      },
      "reason": {
        "default": "Rebind the narrative graph to a successor model revision.",
        "type": "string",
        "minLength": 1,
        "maxLength": 4000
      }
    },
    "required": [
      "requestId",
      "graphHash",
      "modelHash"
    ],
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false
  },
  "annotations": {
    "readOnlyHint": false,
    "destructiveHint": false,
    "idempotentHint": true,
    "openWorldHint": false
  }
}
